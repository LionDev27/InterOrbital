using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace InterOrbital.BuildingSystem
{
    /*public class ContstructionLayer : TilemapLayer
    {
        public void Build(Vector3 worldCoords, BuildableItemSO item)
        {
            Vector3Int coords = _tilemap.WorldToCell(worldCoords);
            if (item.Tile != null)
            {
                _tilemap.SetTile(coords, item.Tile);
            }
        }*/
    }
